<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uffs.cowapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".place.PlaceCadActivity" />
        <activity
            android:name=".place.PlaceCadListActivity"
            android:label="@string/title_activity_place_cad_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".placeLocation.PlaceLocationSearchActivity" />
        <activity android:name=".placeLocation.PlaceLocationListActivity" />
        <activity android:name=".placeLocation.PlaceLocationDetailsActivity" />
        <activity android:name=".place.PlaceCadListItemActivity" />
        <activity android:name=".placeLocation.PlaceLocationListItemActivity"></activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>